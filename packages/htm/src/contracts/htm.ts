/**
 * Path402HTM Smart Contract — Implementation moved to private repo for security.
 * Stub class remains for compilation.
 */

const MOVED = 'Path402HTM contract moved to private repo (Claw-Miner-App)';

export class Path402HTM {
    static loadArtifact(_path: string): void { throw new Error(MOVED); }
    static fromUTXO(_utxo: any): Path402HTM { throw new Error(MOVED); }
    constructor() { throw new Error(MOVED); }
}
